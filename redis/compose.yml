services:
  redis:
    build:
      context: ..
      dockerfile: redis/Dockerfile
    image: vortex-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis:/data

volumes:
  redis:
