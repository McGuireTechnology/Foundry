services:
  vue-nginx:
    build:
      context: .
      dockerfile: web/vue-nginx.Dockerfile
    restart: unless-stopped
    depends_on:
      fastapi:
        condition: service_started
    ports:
      - "5173:80"
