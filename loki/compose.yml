services:
  loki:
    profiles: ["loki"]
    image: vortex-loki
    build:
      context: .
      dockerfile: Dockerfile
      args:
        LOKI_VERSION: ${LOKI_VERSION:-3.2.1}
    restart: unless-stopped
    command: -config.file=/etc/loki/local-config.yaml
    ports:
      - "3100:3100"
